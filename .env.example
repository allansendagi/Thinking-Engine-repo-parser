ANTHROPIC_API_KEY=

# Optional overrides -- defaults in src/providers/anthropic.ts have NOT been verified against a
# live call in this environment. Check current Anthropic API docs before trusting the defaults.
# ANTHROPIC_FAST_MODEL=claude-haiku-4-5-20251001
# ANTHROPIC_STRONG_MODEL=claude-sonnet-4-5

# Optional: where the MCP server (bun run mcp) reads/writes its SQLite database.
# THREAD_DB_PATH=data/thread.db
